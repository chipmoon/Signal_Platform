# Core data
pandas>=2.0
numpy>=1.24
yfinance>=0.2.31

# ML (CPU-only, no torch required)
scikit-learn>=1.3
lightgbm>=4.0

# Visualization
matplotlib>=3.7
plotly>=5.18

# Dashboard
streamlit>=1.30

# Utilities
requests>=2.31
tabulate>=0.9
loguru>=0.7
python-dotenv>=1.0
vnstock>=0.2.8

# NLP — commented out to reduce build time on HF Spaces
# The app has a built-in keyword fallback when transformers is unavailable.
# Uncomment below only if FinBERT sentiment is required:
# transformers>=4.30
# sentencepiece>=0.1.99
